import FullWidthImage from '@/components/FullWidthImage';
import { Container } from 'react-bootstrap';

const FullWidthImagePage = () => (
  <main>
    <Container fluid className="p-0">
      <FullWidthImage />
    </Container>
  </main>
);

export default FullWidthImagePage;
